# nrebuild

A tiny monorepo rebuild-selector. Given the set of file paths that changed
since the last successful build and a manifest of build targets, `nrebuild`
decides the minimal set of targets that must be rebuilt.

